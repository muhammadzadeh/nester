export enum CaptchaProvider {
  RE_CAPTCHA = 'recaptcha',
  H_CAPTCHA = 'hcaptcha',
}

export const CAPTCHA_CUSTOM_HEADER_KEY = 'captcha-token';

export const enum CaptchaMetaKey {
  CAPTCHA = 'GUARD:KEY:CAPTCHA',
}
